# Leave unset to call the API same-origin (e.g. "/api/chat").
# Set this if the frontend and backend are served from different origins.
VITE_API_BASE_URL=

# Optional: only needed if a client-side Google Sign-In widget is used
# instead of a server-driven OAuth redirect.
VITE_GOOGLE_CLIENT_ID=
